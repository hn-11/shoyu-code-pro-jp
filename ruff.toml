# Pin the lint rule set so new ruff defaults don't break CI unannounced.
[lint]
select = ["E4", "E7", "E9", "F", "I", "SIM", "PLW", "EXE"]
# Broad `except Exception` is deliberate in build.py (per-face failure
# collection, calcBounds guard) — leave BLE unselected.
