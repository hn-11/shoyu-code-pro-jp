dist/
# Downloaded upstream font sources: a local build may use the singular
# "upstream/", CI's fetch-upstreams action uses plural "upstreams/".
# Ignore font binaries under both spellings, at any depth.
upstream/**/*.otf
upstream/**/*.ttf
upstreams/
__pycache__/
